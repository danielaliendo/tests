describe('compare strings', () => {
    test('both messages are the same!', () => {

        // initialization
        const message1 = 'Hellow world'

        // stimulus
        const message2 = message1

        // see if the behavior is as expected
        expect(message1).toBe(message2)
 
    })
})